{
  "$schema": "https://github.com/fastfetch-cli/fastfetch/raw/master/doc/json_schema.json",

  "logo": {
    "source": "auto",
    "type": "small",
    "color": {
      "1": "{{colors.secondary_container.default.hex}}",
      "2": "{{colors.tertiary.default.hex}}"
    }
  },

  "display": {
    "separator": "  ",
    "key": {
      "width": 13
    },
    "color": {
      "separator": "{{colors.primary.default.hex}}"
    }
  },

  "modules": [
    "break",
    {
      "type": "title",
      "key": "󰋜 user",
      "color": {
        "user": "{{colors.primary.default.hex}}",
        "at": "{{colors.on_background.default.hex}}",
        "host": "{{colors.primary.default.hex}}"
      },
     "keyColor": "{{colors.primary.default.hex}}" 
    },
    {
      "type": "os",
      "key": "󰌽 os",
      "keyColor": "{{colors.primary.default.hex}}",
      "format": "{name}"
    },
    {
      "type": "kernel",
      "key": "󰒓 kernel",
      "keyColor": "{{colors.primary.default.hex}}",
      "format": "{release}"
    },

    "break",

    {
      "type": "wm",
      "key": "󱂬 wm",
      "format": "niri",
      "keyColor": "{{colors.primary.default.hex}}"
    },
    {
      "type": "shell",
      "key": " shell",
      "format": "bash",
      "keyColor": "{{colors.primary.default.hex}}"
    },
    {
      "type": "terminal",
      "key": " terminal",
      "format": "kitty",
      "keyColor": "{{colors.primary.default.hex}}"
    },

    "break",

    {
      "type": "cpu",
      "key": "󰍛 cpu",
      "keyColor": "{{colors.primary.default.hex}}"
    },
    {
      "type": "memory",
      "key": " memory",
      "keyColor": "{{colors.primary.default.hex}}"
    },
    {
      "type": "disk",
      "key": "󰋊 disk",
      "keyColor": "{{colors.primary.default.hex}}",
      "folders": "/",
      "format": "{size-used} / {size-total} ({size-percentage})"
    },

    "break",

    {
      "type": "terminalfont",
      "key": " font",
      "keyColor": "{{colors.primary.default.hex}}",
      "format": "{name}"
    },
    {
      "type": "uptime",
      "key": "󰥔 uptime",
      "keyColor": "{{colors.primary.default.hex}}"
    },
    {
      "type": "colors",
      "key": "󰏘 colors",
      "keyColor": "{{colors.primary.default.hex}}",
      "symbol": "circle"
    }
  ]
}
